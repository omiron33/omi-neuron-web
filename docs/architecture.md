# Architecture

omi-neuron-web includes:
- Storage layer (PostgreSQL + pgvector)
- Analysis engine (embeddings, clustering, inference)
- API layer (routes + repositories)
- Visualization (Three.js)
- React provider and hooks
