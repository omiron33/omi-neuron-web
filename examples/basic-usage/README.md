# Basic Usage Example

Demonstrates core features of omi-neuron-web.

## Quick Start

1. Install dependencies: `pnpm install`
2. Set up env: `cp .env.example .env.local`
3. Start DB: `pnpm neuron:db:up`
4. Run dev server: `pnpm dev`
