# Ingestion examples

This folder shows minimal connector ingestion usage (CLI + programmatic).

## CLI

```bash
omi-neuron ingest markdown --source docs --path ./docs
omi-neuron ingest markdown --source docs --path ./docs --dry-run
```

## Programmatic

See `examples/ingestion/ingest-markdown.ts`.

